pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'ls -l'
                sh 'pwd'
                echo 'BBB root dir'
            }
        }
    }
}
